import { colors, gradients, getAQIColor, getAQILevel, getAQIDescription, getAQIRecommendations } from './colors';
import { typography, textStyles } from './typography';
import { spacing, borderRadius, shadows } from './spacing';

export { colors, gradients, getAQIColor, getAQILevel, getAQIDescription, getAQIRecommendations } from './colors';
export { typography, textStyles } from './typography';
export { spacing, borderRadius, shadows } from './spacing';

export const theme = {
  colors,
  gradients,
  typography,
  textStyles,
  spacing,
  borderRadius,
  shadows,
  getAQIColor,
  getAQILevel,
  getAQIDescription,
  getAQIRecommendations,
};
